---
title: Correctness
description: Correctness and build-output benchmark report.
---

Use the correctness benchmark before trusting timing numbers.

```sh
pnpm bench:correctness
```

This report builds each benchmark app, verifies that the rendered DOM and
computed styles match the expected correctness checks, records bundle/CSS output
size, and then runs timings only for apps that pass correctness.

The default app set is intentionally limited to production-style comparison
lanes. Fluentic no-hoist, inline dynamic style creation, and runtime-only
css-prop variants are internal diagnostic lanes; select them explicitly with
`APP=...` or include them with `INCLUDE_INTERNAL=1`.

## What It Checks

| Check | Why it matters |
| --- | --- |
| Production build | The benchmark should measure production output, not dev-server behavior. |
| DOM structure | Apps need to render the same shape before timing comparisons make sense. |
| Computed styles | The visible result needs to match, not only the component tree. |
| JS and CSS bytes | Build output can regress even when timing stays steady. |
| Style tags and CSS rules | Runtime insertion behavior should stay visible in the report. |

## Latest Local Report

Report file: `benchmark/main/results/correctness-1783125024848.json`

Created: `2026-07-04T00:30:24.847Z`

Settings: 20 rows, 1 repeat. All rows shown below passed correctness.

| App | Build ms | JS KB | CSS KB |
| --- | ---: | ---: | ---: |
| fluentic-style-extract-direct | 1429.0 | 251.8 | 2.9 |
| fluentic-style-extract-scoped | 1429.0 | 251.8 | 2.9 |
| fluentic-style-extract-token | 1429.0 | 251.8 | 2.9 |
| emotion | 577.0 | 214.5 | 0.0 |
| styled-components | 568.0 | 222.4 | 0.0 |
| goober | 565.0 | 199.3 | 0.0 |
| stylex | 815.0 | 196.2 | 2.1 |
| vanilla-extract | 840.0 | 193.3 | 1.2 |
| css-modules | 580.0 | 193.5 | 1.3 |

## Filtering

```sh
APP=fluentic-style-extract-direct pnpm bench:correctness
APP=@benchmark/app-emotion pnpm bench:correctness
SKIP_FLUENTIC_STYLE=1 pnpm bench:correctness
INCLUDE_INTERNAL=1 pnpm bench:correctness
INCLUDE_EXPERIMENTAL=1 pnpm bench:correctness
```

The `APP` filter accepts benchmark app names and package filters. Use it when a
single app fails and you want a tight repair loop.
